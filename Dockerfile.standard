# Non-optimized Dockerfile using full Node.js image
# This version is NOT recommended for production
FROM node:18

WORKDIR /app

# Copy all files at once (including development files)
COPY . .

# Install all dependencies (including dev dependencies)
RUN npm install

# Expose port
EXPOSE 3000

# Start application
CMD ["npm", "start"]
